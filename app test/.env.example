# Base URL of the CV Parsing API (local or deployed).
VITE_API_BASE_URL=http://localhost:8080

# Optional. Set if the API requires Bearer auth (e.g. API_AUTH_TOKEN on the server).
# VITE_API_AUTH_TOKEN=your-token
