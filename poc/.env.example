# Base URL of the CV Parsing API (local or deployed).
# Local: http://localhost:8080
# Deployed: use your Cloud Run URL, e.g. https://cv-parsing-api-xxxxx-ew.a.run.app
VITE_API_BASE_URL=http://localhost:8080

# Optional. Set if the API requires Bearer auth (API_AUTH_TOKEN on the server).
# VITE_API_AUTH_TOKEN=your-token
