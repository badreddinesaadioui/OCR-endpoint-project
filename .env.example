# API keys for CV Parsing (uvicorn). Copy this file to .env and fill in your keys.
# Optional: if .env is missing, the API also reads .streamlit/secrets.toml.

MISTRAL_API_KEY=your-mistral-key
REPLICATE_API_KEY=your-replicate-token

# Optional: Bearer token for /v1/parse-cv and /v1/jobs (leave unset to allow unauthenticated calls)
# API_AUTH_TOKEN=your-internal-token
